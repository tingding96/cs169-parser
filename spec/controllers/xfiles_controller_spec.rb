require 'rails_helper'
require 'spec_helper'

describe XfilesController do
  # describe 'destroy' do
  #   it 'destroys the movie' do
  #
  #     expect{ delete :destroy, {id: 'Unknown'}}.to raise_error(ActiveRecord::RecordNotFound)
  #   end
  # end

  # describe 'upload json' do
  #   it 'upload json' do
  #     expect(:post => "/xfiles").to route_to(:controller => "xfiles", "action" => "create")
  #     post "create", {:xfile => {:name => "BNMRNF"}}
  #     expect(flash[:notice]).to match(/(.*) was successfully created./)
  #   end
  # end
  #
  # def test_making_order
  # book = Book.new(:title => “RSpec Intro”, :price => 20)
  # customer = Customer.new
  # order = Order.new(customer, book)
  #
  # order.submit
  #
  # assert(customer.orders.last == order)
  # assert(customer.ordered_books.last == book)
  # assert(order.complete?)
  # assert(!order.shipped?)
  # end
  #
end
